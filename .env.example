# Supabase Configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (safe to use in frontend)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Setup Instructions:
# 1. Create a Supabase account at https://supabase.com
# 2. Create a new project
# 3. Go to Settings > API to find your URL and anon key
# 4. Copy this file to .env and fill in your actual values
# 5. Create a table called 'contact_submissions' with these columns:
#    - id (uuid, primary key, default: gen_random_uuid())
#    - created_at (timestamp, default: now())
#    - name (text, required)
#    - email (text, required)
#    - message (text, required)
#    - phone (text, optional)
#    - company (text, optional)
#    - interested_in (text, optional)

# Email (Resend) - Required for /api/contact
# Get API key: https://resend.com/api-keys
# FROM_EMAIL must be from a verified domain in Resend (e.g. "Lead Form <no-reply@yourdomain.com>")
# You may use "onboarding@resend.dev" for testing but it has delivery restrictions.
RESEND_API_KEY=your-resend-api-key
FROM_EMAIL=Lead Form <onboarding@resend.dev>
TO_EMAIL=you@example.com

# Hugging Face Configuration
# Get API token: https://huggingface.co/settings/tokens
# Used for AI image generation and virtual design tools
HUGGINGFACE_API_TOKEN=your_huggingface_token_here

# Hugging Face API Key for Edge Functions (Free tier - 1000 req/day)
# Same token as above, just different env var name for Vercel Edge Functions
HF_API_KEY=your_huggingface_token_here
